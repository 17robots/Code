import * as Constants from './constants'

let initial = {
    searched: false,
    searchText: '',
    results: [],
    resultType: ''
}

export default reducer = (state = initial, action) => {
    switch(action.type) {
        case Constants.
    }
}