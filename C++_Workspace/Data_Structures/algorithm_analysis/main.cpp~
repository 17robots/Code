#include <iostream>

int main() {
    int sum = 0;
    int n = 1000;
    for(int i = 0; i < n; ++i) {
        for(int j = 0; j < i; ++j) {
	    ++sum;
	}
    }

    std::cout << "Sum is " << sum;
    return 0;
}
